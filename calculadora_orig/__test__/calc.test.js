const{init, limpiar, resetear, resolver} = require('../funcionalidad.js')


describe('Pruebas limpiar', () => {
    test('Limpia los elementos ingresados', () =>{
        //armo un cte que ingrese los elementos requeridos (algun numero)
        //y al onclick de c, se eliminan los elementos 
        expect().toBe('');   
    })
})